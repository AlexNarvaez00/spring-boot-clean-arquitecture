package mx.edu.itoaxaca.api.v1.{{name}}.application;

import static org.junit.jupiter.api.Assertions.assertEquals;

import java.util.ArrayList;
import mx.edu.itoaxaca.api.v1.{{name}}.domain.{{Name}};
import mx.edu.itoaxaca.api.v1.{{name}}.infrastructure.persistence.{{Name}}RepositoryMock;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageImpl;
import org.springframework.data.domain.PageRequest;

public class {{Name}}GetAllTest {
    @Test
    void runTest() {
        var creator = new {{Name}}GetAll();

        var repo = {{Name}}RepositoryMock.mock;

        PageRequest pageReq = PageRequest.of(0, 2);
        Page<{{Name}}> {{name}}s = new PageImpl<{{Name}}>(new ArrayList<{{Name}}>(), pageReq, 0);

        Mockito.when(repo.findByDeletedAtIsNull(pageReq)).thenReturn({{name}}s);
        Page<{{Name}}> all = {{Name}}GetAll.run(pageReq, repo);
        assertEquals(all.getSize(), 2);
    }
}
