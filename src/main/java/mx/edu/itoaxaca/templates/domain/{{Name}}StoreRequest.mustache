package mx.edu.itoaxaca.api.v1.{{name}}.domain;

import java.util.UUID;

import jakarta.validation.constraints.NotNull;
import lombok.Data;

@Data
public class {{Name}}StoreRequest {
    private UUID id;

    @NotNull
    private String name;

    public {{Name}} as{{Name}}() {
        return new {{Name}}(
            (this.id == null) ? null : UUID.randomUUID(),
            new {{Name}}Name(this.name)
        );
    }
}
